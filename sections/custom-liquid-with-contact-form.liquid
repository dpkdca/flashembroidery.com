<style>
  .section-template--15341208174664__custom_liquid_with_contact_form_DRLUbj-padding {
    display: flex;
    column-gap: 7%;
}
 section-template--15341208174664__custom_liquid_with_contact_form_DRLUbj-padding .part-1 H1{
   /* TEXT-ALIGN:CENTER; */
 }
 @media screen and (max-width :700px){
   .section-template--15341208174664__custom_liquid_with_contact_form_DRLUbj-padding {
    display: BLOCK;
   
   }
 } 

  
</style>
{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: calc({{ section.settings.padding_top }}px * 0.75);
    padding-bottom: calc({{ section.settings.padding_bottom }}px  * 0.75);
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}
<div class="page-width color-{{ section.settings.color_scheme }} gradient">
  <div class="section-{{ section.id }}-padding">
      
    <div class="part-1">
        
        {% if section.settings.title != blank %}
        <h1>{{ section.settings.title }}</h1>
          {% endif %}
        {% if section.settings.richtext != blank %}
            <p> {{ section.settings.richtext }}</p> 
          {% endif %}
        </div>
<div class="part-2">
    {{ section.settings.custom_liquid }}
</div>  
</div>
</div>

{% schema %}
{
  "name": "custom-liquid-with-cont",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "title",
      "info": "title"
    },
    {
      "type": "richtext",
      "id": "richtext",
      "label": "richtext",
      "info": "richtext"
    },
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "t:sections.custom-liquid.settings.custom_liquid.label",
      "info": "t:sections.custom-liquid.settings.custom_liquid.info"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 52
    }
  ],
  "presets": [
    {
      "name": "custom-liquid-with-cont"
    }
  ]
}
{% endschema %}
